description = "Main entry point for SDD design."
prompt = "Activate the 'sdd-design-engine' skill and follow its instructions to intelligently determine the next design step for the feature: {{args}}"