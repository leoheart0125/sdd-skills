{
    "architecture_style": "{{ style }}",
    "components": [
        {
            "name": "{{ ComponentName }}",
            "type": "{{ ComponentType }}",
            "description": "{{ description }}"
        }
    ],
    "data_flow": [
        {
            "source": "{{ source }}",
            "destination": "{{ destination }}",
            "description": "{{ description }}"
        }
    ],
    "decision_log": []
}