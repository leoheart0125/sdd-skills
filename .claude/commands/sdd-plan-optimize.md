<!-- Description: Re-sort tasks based on dependencies. -->

Read the orchestration rules in AGENT.md. Delegate to the Plan Agent (agents/plan-agent.md) as per AGENT.md instructions.

Command: /sdd-plan-optimize
User args: $ARGUMENTS

Before delegating, read .sdd/context/context.json for current state. Tell the Plan Agent to optimize the existing task order.